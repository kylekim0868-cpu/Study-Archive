# Python — 정렬과 key

## 학습 계기

`Greedy_미팅`에서 회의를 종료 시간 순으로 검사하기 위해 작성했다.

## 확인할 개념

- `(start, end)` 튜플에 대한 기본 정렬 순서
- `list.sort()`가 원본 리스트에 미치는 영향
- `key` 함수가 받아야 하는 값과 반환해야 하는 값
- 일반 함수와 `lambda`로 정렬 기준 표현하기
- 함수 객체 `get_end`와 함수 호출 `get_end()`의 차이

## 내가 설명해 보기

학습 후 직접 예제와 설명을 작성한다.

## 관련 문제

- [Greedy 미팅 문제점 기록](../문제모음집/09_greedy_meeting.md)
